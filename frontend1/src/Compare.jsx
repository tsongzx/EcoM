import React from "react";
/**
 * @param {Array} companies takes in an array of company id's to compare
 */
const Compare = ( {companies} ) => {
    //fetch data for each company
}

export default Compare;